#include "pxt.h"

using namespace pxt;

//% weight=10 color=#9F79EE icon="\uf108" block="bluebox"
namespace bluebox {



    // /**
    //  * Scroll a number on the screen. If the number fits on the screen (i.e. is a single digit), do not scroll.
    //  * @param interval speed of scroll; eg: 150, 100, 200, -100
    //  */
    // //% help=bluebox/show-number
    // //% weight=96
    // //% blockId=bluebox_show_number block="show|number %number" blockGap=8
    // void showNumber(int value, int interval = 150) {
    //   if (interval <= 0)
    //     interval = 1;
    //   ManagedString t(value);
    //   if (value < 0 || value >= 10) {
    //     uBit.display.scroll(t, interval);
    //   } else {
    //     uBit.display.printChar(t.charAt(0), interval * 5);
    //   }
    // }

}